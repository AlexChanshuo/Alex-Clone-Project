# Example Hermes config for Alexma Clone.
# Copy/adapt into $HERMES_HOME/config.yaml after Hermes is installed.

agent:
  name: Alexma Clone

terminal:
  backend: local
  cwd: "/Users/alex/Documents/New project/Alex-Clone-Project"

platforms:
  telegram:
    enabled: true

# Keep tool access conservative. The Alex Clone backend should perform
# deterministic writes/checkpoints through its CLI.
tools:
  terminal:
    enabled: true

